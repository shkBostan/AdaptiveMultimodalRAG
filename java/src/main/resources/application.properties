# Application Configuration
app.name=AdaptiveMultimodalRAG
app.version=1.0.0

# Model Configuration
model.bert.path=./models/bert-base-uncased
model.embedding.dim=768

# Retrieval Configuration
retrieval.top.k=5
retrieval.threshold=0.7

