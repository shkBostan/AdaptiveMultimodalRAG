# Test Configuration

api:
  host: localhost
  port: 8001
  debug: true

database:
  url: postgresql://localhost:5432/ragdb_test
  pool_size: 2

models:
  bert:
    path: ./models/bert-base-uncased
    cache_dir: ./cache/models
  embedding_dim: 768

retrieval:
  top_k: 3
  threshold: 0.5
  index_type: L2

logging:
  level: WARNING
  file: ./data/logs/test.log

